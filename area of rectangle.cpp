//Take the length(L) and breadth(B) of the rectangle as input and find its area.
#include<iostream>
using namespace std;
int main(){
  int l,b,area;
  cin>>l,b;
  area=l*b;
  cout<<area;
  return 0;
}
